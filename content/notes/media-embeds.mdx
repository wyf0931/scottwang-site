---
title: "博客 MDX 组件速查"
description: "写文章时有哪些 MDX 组件可用，每个组件的语法和参数。"
date: "2026-08-03"
type: "notes"
tags:
  - MDX
  - 博客
  - 写作
draft: false
---

写文章时除了标准 Markdown 语法，博客还支持一些 MDX 组件和扩展功能。这篇速查手册把这些组件的写法集中整理，方便直接复制粘贴。

## 视频嵌入

博客提供了 `YouTubeEmbed` 和 `BilibiliEmbed` 两个组件，内部都是 iframe，自动做响应式和 lazy loading。

YouTube 视频嵌入：

```mdx
<YouTubeEmbed url="https://www.youtube.com/watch?v=dQw4w9WgXcQ" title="示例 YouTube 视频" />
```

B 站视频嵌入：

```mdx
<BilibiliEmbed url="https://www.bilibili.com/video/BV1GJ411x7h7" title="示例 B 站视频" />
```

`url` 是必填参数。`title` 可选，用于无障碍标注，不填的话默认显示 "YouTube video" 或 "Bilibili video"。YouTube 同时支持 `youtube.com/watch?v=` 和 `youtu.be/` 两种链接格式，B 站支持标准的 BV 号 URL。

## 提示框 Callout

Callout 用来在文中插入需要读者注意的信息，渲染为带图标的 `<aside>` 块。

```mdx
<Callout tone="info">这是一条信息提示</Callout>
```

```mdx
<Callout tone="warning">这是一条警告提示</Callout>
```

```mdx
<Callout tone="success">这是一条成功提示</Callout>
```

<Callout tone="info">tone 的默认值是 info，所以不写 tone 也行。</Callout>

`tone` 支持三个值：`info`（默认）、`warning`、`success`。内容写在标签之间，可以包含行内 Markdown。

## Mermaid 图表

用 ` ```mermaid ` 开头的代码块会被自动渲染为 SVG 图表，客户端执行，支持暗色主题。

```mermaid
graph TD
title 示例流程图
    A[开始] --> B{判断条件}
    B -->|是| C[执行操作]
    B -->|否| D[结束]
```

上图的源码是这样写的：

````mdx
```mermaid
graph TD
title 示例流程图
    A[开始] --> B{判断条件}
    B -->|是| C[执行操作]
    B -->|否| D[结束]
```
````

支持所有 Mermaid 图表类型：graph（流程图）、sequence（时序图）、class（类图）、state（状态图）、er（ER 图）、gantt（甘特图）、pie（饼图）、packet（报文图）等。用 `title` 关键字在图表定义内写标题。

## 代码高亮

所有代码块通过 rehype-pretty-code 渲染，自动带上语法高亮和复制按钮。写法就是标准 Markdown 代码块，语言标识决定高亮方案：

```typescript
const greeting = "Hello, MDX!";
console.log(greeting);
```

````mdx
```typescript
const greeting = "Hello, MDX!";
console.log(greeting);
```
````

支持的语言标识跟 Shiki 一致，常见的 `javascript`、`typescript`、`python`、`rust`、`go`、`bash`、`json`、`yaml` 等都能识别。

## GitHub 仓库卡片

如果文章的 frontmatter 里写了 `github` 字段，文章顶部会自动展示一个 GitHub 仓库信息卡片，显示仓库名、描述、星标数、主要语言和 fork 数。

```yaml
---
github: "ohmyscott/ohmyagent"
---
```

这个功能写在 frontmatter 里就行，不需要在正文里额外添加 MDX 组件。适合那些围绕某个开源项目展开的笔记。
